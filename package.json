{
  "name": "cc-cockpit",
  "version": "0.1.0",
  "private": true,
  "description": "Personal multi-session Claude Code cockpit",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test --test-force-exit --test-timeout=30000"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.195",
    "ws": "^8.18.0"
  }
}
