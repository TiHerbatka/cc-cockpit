# cc-cockpit — GUI glossary (generated)

A map of the cockpit GUI surface, **auto-discovered from the live GUI** by the `/gui-map` skill — **do not edit by hand** (a re-run overwrites it). Element handles and names are derived mechanically from the DOM, so they are functional rather than carefully worded. Every area/element is keyed by a `GUI-<AREA>-<slug>` handle for cross-reference.

**Last generated: 2026-06-30**

Visual map (hover/click hotspots): [gui-map/map.html](gui-map/map.html).

## SIDEBAR — The left rail: sessions grouped by project, the create/resume actions, and the error center.

### GUI-SIDEBAR-session-list — Session List
The "Session List" element.

### GUI-SIDEBAR-alpha — alpha
The "alpha" element.

### GUI-SIDEBAR-idle — Idle
The "Idle" element.

### GUI-SIDEBAR-alpha-new-1 — alpha new 1
The "alpha new 1" element.

### GUI-SIDEBAR-working — Working
The "Working" element.

### GUI-SIDEBAR-alpha-new-2 — alpha new 2
The "alpha new 2" element.

### GUI-SIDEBAR-beta — beta
The "beta" element.

### GUI-SIDEBAR-your-move — Your Move
The "Your Move" element.

### GUI-SIDEBAR-beta-new-1 — beta new 1
The "beta new 1" element.

### GUI-SIDEBAR-needs-you — Needs You
The "Needs You" element.

### GUI-SIDEBAR-beta-new-2 — beta new 2
The "beta new 2" element.

### GUI-SIDEBAR-gamma — gamma
The "gamma" element.

### GUI-SIDEBAR-gamma-new-1 — gamma new 1
The "gamma new 1" element.

### GUI-SIDEBAR-gamma-new-2 — gamma new 2
The "gamma new 2" element.

### GUI-SIDEBAR-gamma-new-3 — gamma new 3
The "gamma new 3" element.

### GUI-SIDEBAR-temporary — Temporary
The "Temporary" element.

### GUI-SIDEBAR-exited — Exited
The "Exited" element.

### GUI-SIDEBAR-quick-experiment — quick-experiment
The "quick-experiment" element.

### GUI-SIDEBAR-other — Other
The "Other" element.

### GUI-SIDEBAR-legacy-thing — legacy-thing
The "legacy-thing" element.

### GUI-SIDEBAR-new-session — + New session
The "+ New session" button.

### GUI-SIDEBAR-resume — Resume…
The "Resume…" button.

### GUI-SIDEBAR-error — Error
The "Error" element.

### GUI-SIDEBAR-gui-errors — GUI errors
The "GUI errors" button.

### GUI-SIDEBAR-1 — 1
The "1" element.

### GUI-SIDEBAR-kill-and-remove — Kill and remove
The "Kill and remove" button.

### GUI-SIDEBAR-error-panel — Error Panel
The "Error Panel" element.

### GUI-SIDEBAR-clear — clear
The "clear" button.

### GUI-SIDEBAR-error-close — Error Close
The "Error Close" button.

### GUI-SIDEBAR-error-list — Error List
The "Error List" element.

### GUI-SIDEBAR-err-time — Err Time
The "Err Time" element.

### GUI-SIDEBAR-demo-preview-failed-to-render — Demo: preview failed to render
The "Demo: preview failed to render" element.

### GUI-SIDEBAR-stack — stack
The "stack" button.

## HEADER — The bar above the focused session: its name/state, the panel buttons, the usage chip, and the controls.

### GUI-HEADER-idle — Idle
The "Idle" element.

### GUI-HEADER-alpha-new-1 — alpha new 1
The "alpha new 1" element.

### GUI-HEADER-in-session-todos-this-session-s-live-tas — In-session todos (this session's live task list)
The "In-session todos (this session's live task list)" button.

### GUI-HEADER-topics-tracked-for-this-session — Topics tracked for this session
The "Topics tracked for this session" button.

### GUI-HEADER-permission-mode-click-to-change — Permission mode — click to change
The "Permission mode — click to change" element.

### GUI-HEADER-model — Model
The "Model" select.

### GUI-HEADER-reasoning-effort — Reasoning effort
The "Reasoning effort" select.

### GUI-HEADER-open-local-docs-md — Open local-docs.md
The "Open local-docs.md" button.

### GUI-HEADER-show-todo-md-entries — Show TODO.md entries
The "Show TODO.md entries" button.

### GUI-HEADER-working — Working
The "Working" element.

### GUI-HEADER-alpha-new-2 — alpha new 2
The "alpha new 2" element.

### GUI-HEADER-interrupt-esc — Interrupt (Esc)
The "Interrupt (Esc)" button.

### GUI-HEADER-beta-new-2 — beta new 2
The "beta new 2" element.

### GUI-HEADER-gamma-new-1 — gamma new 1
The "gamma new 1" element.

### GUI-HEADER-gamma-new-2 — gamma new 2
The "gamma new 2" element.

### GUI-HEADER-gamma-new-3 — gamma new 3
The "gamma new 3" element.

## CONV — The focused session's conversation log and the status line above it.

### GUI-CONV-todos-1-3 — todos 1/3
The "todos 1/3" element.

### GUI-CONV-gui-user — Gui User
The "Gui User" element.

### GUI-CONV-tool-in — Tool In
The "Tool In" element.

### GUI-CONV-tool-out — Tool Out
The "Tool Out" element.

### GUI-CONV-add-the-toggle-markup-to-the-settings-pa — ✓ Add the toggle markup to the settings panel
The "✓ Add the toggle markup to the settings panel" element.

### GUI-CONV-persist-the-choice-to-localstorage — ▸ Persist the choice to localStorage
The "▸ Persist the choice to localStorage" element.

### GUI-CONV-apply-the-dark-theme-on-load — ○ Apply the dark theme on load
The "○ Apply the dark theme on load" element.

### GUI-CONV-grep-readfilesync — ⚙︎ Grep readFileSync
The "⚙︎ Grep readFileSync" element.

### GUI-CONV-md-p — Md P
The "Md P" element.

### GUI-CONV-refactor-the-scanner-now — Refactor the scanner now.
The "Refactor the scanner now." element.

### GUI-CONV-waiting-for-activity — waiting for activity…
The "waiting for activity…" element.

### GUI-CONV-investigate-the-flaky-transcript-test-an — Investigate the flaky transcript test and propose a fix.
The "Investigate the flaky transcript test and propose a fix." element.

## COMPOSE — The message box at the bottom of the focused session.

### GUI-COMPOSE-gui-compose-input — Gui Compose Input
The "Gui Compose Input" element.

### GUI-COMPOSE-send — Send
The "Send" button.

### GUI-COMPOSE-match-this-screen-image-1-please — Match this screen: [Image #1] please.
The "Match this screen: [Image #1] please." element.

### GUI-COMPOSE-img-token — Img Token
The "Img Token" element.

## PANEL — Floating panels over the conversation (topics / in-session todos / TODO.md).

### GUI-PANEL-floating-panel — Floating panel
The "Floating panel" element.

### GUI-PANEL-topics — Topics
The "Topics" element.

### GUI-PANEL-close-esc — Close (Esc)
The "Close (Esc)" button.

### GUI-PANEL-tpc1 — TPC1
The "TPC1" element.

### GUI-PANEL-dark-mode-toggle — Dark-mode toggle
The "Dark-mode toggle" element.

### GUI-PANEL-topic-dot — Topic Dot
The "Topic Dot" element.

### GUI-PANEL-add-and-persist-a-dark-mode-setting-in-t — Add and persist a dark-mode setting in the settings panel.
The "Add and persist a dark-mode setting in the settings panel." element.

### GUI-PANEL-tpc2 — TPC2
The "TPC2" element.

### GUI-PANEL-settings-persistence — Settings persistence
The "Settings persistence" element.

### GUI-PANEL-audit-that-every-setting-survives-a-relo — Audit that every setting survives a reload.
The "Audit that every setting survives a reload." element.

### GUI-PANEL-tpc3 — TPC3
The "TPC3" element.

### GUI-PANEL-flaky-test-triage — Flaky test triage
The "Flaky test triage" element.

### GUI-PANEL-stabilise-the-transcript-timestamp-test — Stabilise the transcript timestamp test.
The "Stabilise the transcript timestamp test." element.

### GUI-PANEL-in-session-todos — In-session todos
The "In-session todos" element.

### GUI-PANEL-add-the-toggle-markup-to-the-settings-pa — ✓ Add the toggle markup to the settings panel
The "✓ Add the toggle markup to the settings panel" element.

### GUI-PANEL-persist-the-choice-to-localstorage — ▸ Persist the choice to localStorage
The "▸ Persist the choice to localStorage" element.

### GUI-PANEL-apply-the-dark-theme-on-load — ○ Apply the dark theme on load
The "○ Apply the dark theme on load" element.

### GUI-PANEL-todo-md — TODO.md
The "TODO.md" element.

### GUI-PANEL-a-settings-panel — A. Settings panel
The "A. Settings panel" element.

### GUI-PANEL-a1-add-the-dark-mode-toggle-markup — ☑ A1. Add the dark-mode toggle markup
The "☑ A1. Add the dark-mode toggle markup" element.

### GUI-PANEL-a2-persist-the-choice-to-localstorage — ☐ A2. Persist the choice to localStorage
The "☐ A2. Persist the choice to localStorage" element.

### GUI-PANEL-a2-1-read-the-saved-value-on-load — ☐ A2.1. Read the saved value on load
The "☐ A2.1. Read the saved value on load" element.

### GUI-PANEL-a2-2-write-on-toggle — ☑ A2.2. Write on toggle
The "☑ A2.2. Write on toggle" element.

### GUI-PANEL-b-cleanup — B. Cleanup
The "B. Cleanup" element.

### GUI-PANEL-b1-remove-the-legacy-theme-switch — ☐ B1. Remove the legacy theme switch
The "☐ B1. Remove the legacy theme switch" element.

## INTERACTION — The blocking modal shown when a session needs a decision from you.

### GUI-INTERACTION-interaction-modal — Interaction modal
The "Interaction modal" element.

### GUI-INTERACTION-permission-requested — Permission requested
The "Permission requested" element.

### GUI-INTERACTION-claude-wants-to-use-bash — Claude wants to use Bash
The "Claude wants to use Bash" element.

### GUI-INTERACTION-interaction-input — Interaction Input
The "Interaction Input" element.

### GUI-INTERACTION-allow-once — Allow once
The "Allow once" button.

### GUI-INTERACTION-allow-don-t-ask-again — Allow, don't ask again
The "Allow, don't ask again" button.

### GUI-INTERACTION-deny — Deny
The "Deny" button.

### GUI-INTERACTION-plan-ready-for-review — Plan ready for review
The "Plan ready for review" element.

### GUI-INTERACTION-approve — Approve
The "Approve" button.

### GUI-INTERACTION-approve-auto-accept-edits — Approve & auto-accept edits
The "Approve & auto-accept edits" button.

### GUI-INTERACTION-keep-planning — Keep planning
The "Keep planning" button.

### GUI-INTERACTION-claude-is-asking — Claude is asking
The "Claude is asking" element.

### GUI-INTERACTION-storage-where-should-the-toggle-state-be — Storage: Where should the toggle state be stored?
The "Storage: Where should the toggle state be stored?" element.

### GUI-INTERACTION-localstorage-per-browser-survives-reload — localStorage — Per-browser, survives reloads
The "localStorage — Per-browser, survives reloads" button.

### GUI-INTERACTION-a-cookie-sent-to-the-server-each-request — A cookie — Sent to the server each request
The "A cookie — Sent to the server each request" button.

### GUI-INTERACTION-in-memory-only-resets-on-reload — In-memory only — Resets on reload
The "In-memory only — Resets on reload" button.

### GUI-INTERACTION-submit — Submit
The "Submit" button.

### GUI-INTERACTION-connect-to-the-design-service — Connect to the design service
The "Connect to the design service" element.

### GUI-INTERACTION-interaction-sub — Interaction Sub
The "Interaction Sub" element.

### GUI-INTERACTION-input — input
The "input" input.

### GUI-INTERACTION-decline — Decline
The "Decline" button.

## MODAL — Centered dialogs: the new-session / resume pickers, rename, and quick preview.

### GUI-MODAL-modal-dialog — Modal dialog
The "Modal dialog" element.

### GUI-MODAL-input — input
The "input" input.

### GUI-MODAL-save — Save
The "Save" button.

### GUI-MODAL-quick-preview-window — Quick-preview window
The "Quick-preview window" element.

### GUI-MODAL-preview-close — Preview Close
The "Preview Close" button.

### GUI-MODAL-waiting-for-activity — waiting for activity…
The "waiting for activity…" element.

### GUI-MODAL-investigate-the-flaky-transcript-test-an — Investigate the flaky transcript test and propose a fix.
The "Investigate the flaky transcript test and propose a fix." element.

### GUI-MODAL-cockpit-projects — Cockpit projects
The "Cockpit projects" button.

### GUI-MODAL-discovered-folders — Discovered folders
The "Discovered folders" button.

### GUI-MODAL-search-by-name-or-path — Search by name or path…
The "Search by name or path…" input.

### GUI-MODAL-older-than-7-days — Older than 7 days ▸
The "Older than 7 days ▸" button.

### GUI-MODAL-1-project-created-but-never-used-click-t — 1 project created but never used — click to list
The "1 project created but never used — click to list" button.

### GUI-MODAL-last-24-hours-2 — Last 24 hours (2)
The "Last 24 hours (2)" element.

### GUI-MODAL-project-row — Project Row
The "Project Row" button.

### GUI-MODAL-alpha — alpha
The "alpha" element.

### GUI-MODAL-proj-row-time — Proj Row Time
The "Proj Row Time" element.

### GUI-MODAL-beta — beta
The "beta" element.

### GUI-MODAL-resume-col-head — Resume Col Head
The "Resume Col Head" element.

### GUI-MODAL-new-project-name — new project name
The "new project name" input.

### GUI-MODAL-create-start — Create & start
The "Create & start" button.

### GUI-MODAL-temporary-session — + Temporary session
The "+ Temporary session" button.

### GUI-MODAL-global-discovery — Global discovery
The "Global discovery" button.

### GUI-MODAL-cockpit — Cockpit
The "Cockpit" button.

### GUI-MODAL-temporary — Temporary
The "Temporary" button.

### GUI-MODAL-search-by-session-project-or-path — Search by session, project, or path…
The "Search by session, project, or path…" input.

### GUI-MODAL-last-24-hours-4 — Last 24 hours (4)
The "Last 24 hours (4)" element.

### GUI-MODAL-proj-path — Proj Path
The "Proj Path" element.

### GUI-MODAL-recent-row — Recent Row
The "Recent Row" button.

### GUI-MODAL-add-a-dark-mode-toggle — Add a dark-mode toggle
The "Add a dark-mode toggle" element.

### GUI-MODAL-recent-time — Recent Time
The "Recent Time" element.

### GUI-MODAL-audit-settings-persistence — Audit settings persistence
The "Audit settings persistence" element.

### GUI-MODAL-fix-the-flaky-transcript-test — Fix the flaky transcript test
The "Fix the flaky transcript test" element.

### GUI-MODAL-quick-experiment — quick-experiment
The "quick-experiment" element.

### GUI-MODAL-quick-scratch-calculation — quick scratch calculation
The "quick scratch calculation" element.

### GUI-MODAL-resume-empty — Resume Empty
The "Resume Empty" element.

## MENU — Transient pop-up menus.

### GUI-MENU-permission-mode-menu — Permission-mode menu
The "Permission-mode menu" element.

### GUI-MENU-default — default
The "default" button.

### GUI-MENU-prompts-for-anything-not-pre-approved — Prompts for anything not pre-approved.
The "Prompts for anything not pre-approved." element.

### GUI-MENU-acceptedits — acceptEdits
The "acceptEdits" button.

### GUI-MENU-auto-accepts-file-edits-other-tools-stil — Auto-accepts file edits; other tools still prompt.
The "Auto-accepts file edits; other tools still prompt." element.

### GUI-MENU-plan — plan
The "plan" button.

### GUI-MENU-explore-and-plan-only-never-executes-edi — Explore and plan only; never executes edits.
The "Explore and plan only; never executes edits." element.

### GUI-MENU-bypasspermissions — bypassPermissions
The "bypassPermissions" button.

### GUI-MENU-approves-everything-that-reaches-the-gat — Approves everything that reaches the gate (your deny-rule…
Approves everything that reaches the gate (your deny-rule…

### GUI-MENU-dontask — dontAsk
The "dontAsk" button.

### GUI-MENU-never-prompts-denies-anything-not-pre-ap — Never prompts; denies anything not pre-approved.
The "Never prompts; denies anything not pre-approved." element.

### GUI-MENU-auto — auto
The "auto" button.

### GUI-MENU-a-model-classifier-approves-or-denies-ea — A model classifier approves or denies each call.
The "A model classifier approves or denies each call." element.
