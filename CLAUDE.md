# cc-cockpit

Personal local web app: a multi-session **Claude Code cockpit**. One window shows all live Claude Code sessions at once; switch between them instantly; type into any of them. The unit is a *session*, not a *project*. Built because Agent View is too terse/slow to switch, kanban tools (Vibe Kanban, AI Agent Board) are task-centric (dispatch → review → merge), and claudecodeui is project-centric — none give a simultaneous-live-sessions cockpit.

## Architecture direction (TOP PRIORITY): built on the Claude Agent SDK

The project is being re-founded on the **Claude Agent SDK** (`@anthropic-ai/claude-agent-sdk`). The cockpit drives each session through the SDK's `query()` — a structured programmatic channel for input, output, and tool-permission/control — instead of screen-driving a `claude` pseudo-terminal. `query()` spawns and owns a local `claude` subprocess and talks to it over stdio; that bundled `claude` authenticates on the **user's own Claude Code subscription** (verified 2026-06-27 via the `five_hour` subscription rate-limit), so the subscription-only posture is preserved. **cc-cockpit relies solely on the user's Claude Code subscription.** The PTY/`node-pty` substrate has been **removed entirely** — cc-cockpit is SDK-only, with no PTY fallback. The re-architecture (TODO section G) is **complete**; rationale and the SDK process model are in `local-docs.md` (§2, §5).

- **The SDK does not replace stream-json — it wraps it.** The SDK speaks the same newline-delimited JSON protocol to its child `claude` over stdio; choosing the SDK means letting it speak that protocol for us rather than hand-rolling the raw CLI. "SDK" and "stream-json" are therefore the same substrate at different layers, not competing options.
- **Env scrub stays mandatory, now enforced through the SDK's `env` option.** The SDK's `env` *replaces* the child environment rather than merging with `process.env`, so the cockpit must hand it the complete, already-scrubbed env (`scrubParentClaudeEnv({ ...process.env })`). Omitting `env` would let the parent Claude Code session's leaked markers (`CLAUDECODE`, `CLAUDE_CODE_*`, …) reach the child and reproduce the no-transcript bug; passing a minimal `env` would instead strip vars the child needs (`PATH`, `USERPROFILE`, …) and break the spawn. Same scrub logic the former PTY path used — only the delivery point (the SDK `env` option) is new.
## Status (as of 2026-06-28)

- ✅ **MVP built and merged to `master`** — design spec (`docs/superpowers/specs/2026-06-16-cc-cockpit-design.md`) + plan (`docs/superpowers/plans/2026-06-16-cc-cockpit-mvp.md`); all 7 spec acceptance criteria verified live in a browser. Run it with `npm start`.
- ✅ **Session-state detection v1 merged to `master`** — hook-driven `needs-you` + acknowledgement (`docs/superpowers/specs/2026-06-17-session-state-detection-design.md`).
- ✅ **Hook-driven session state (v2) merged to `master`** — drives `working`/`idle` from Claude Code turn-boundary hooks (`UserPromptSubmit`/`Stop`/`Notification`) instead of guessing `idle` from output silence, eliminating the mid-turn flicker. Spec `docs/superpowers/specs/2026-06-19-hook-driven-session-state-design.md`, plan `docs/superpowers/plans/2026-06-19-hook-driven-session-state.md`. Live use confirmed the hook pipeline fires end-to-end.
- ✅ **`your-move` state (v2.1)** — a background (unfocused) session that finishes its turn now shows a distinct steady-blue **Your move** state (sidebar group between *Needs you* and *Working*), separate from the amber permission `needs-you`. Added because turn-end on a background session — including Claude ending with a prose question — previously showed `idle` and gave no signal it was waiting for you (Claude Code can't distinguish "asked a question" from "just finished"). No hook/protocol change: `your-move` vs `idle` is derived from focus in the registry. See the "Amendment 2026-06-24" section of the v2 spec. 38/38 tests pass; the new sidebar group was browser-verified.
- ✅ **Projects & live view (Plan 1 of "Discovery & projects")** — the sidebar now groups *live* sessions by **project** (a subfolder of `C:\claude_projects\cockpit`; everything else under "Other"), state shown as the dot, attention states sorted to the top; exited sessions show a red ✕ with a hover button to kill (live) / remove (exited). New sessions start via a **project picker** (create a project + start in it, no path typing) backed by `GET`/`POST /api/projects`. Spec `docs/superpowers/specs/2026-06-24-discovery-and-projects-design.md` (Part A), plan `docs/superpowers/plans/2026-06-24-projects-and-live-view.md`. 54/54 tests pass; browser-verified.
- ✅ **Session discovery & resume (Plan 2 of "Discovery & projects")** — a **Resume…** button opens a modal with **day / 3 days / week** tabs listing past sessions across the machine (scanned from `~/.claude/projects/*/*.jsonl`, grouped by folder, titled from each session's `aiTitle`); clicking one spawns `claude --resume <id>` in that session's original cwd as a new live session. Backed by `server/recent.js` + `GET /api/recent` + WS `resume`; `buildSpawn` gained `--resume`. Plan `docs/superpowers/plans/2026-06-24-session-discovery-and-resume.md`. 61/61 tests pass; browser-verified end-to-end. **The "Discovery & projects" feature is complete.**
- ✅ **Terminal focus fix + passive session preview (merged to `master`)** — (1) clicking a session in the sidebar now moves keyboard focus to the terminal, so keystrokes (incl. arrow-key history recall) reach Claude without first clicking into the pane. (2) Right-click a sidebar session → **Quick preview**: a read-only modal that mirrors the session live via a side-effect-free `peek` WS message (never focuses/acknowledges/resizes the PTY) + the existing `output` broadcast; the preview terminal is sized to the session's PTY grid so a full-screen TUI replays correctly (else it rendered empty), and the font is scaled so the whole grid fits the (enlarged) modal with no scrollbars. Ring buffer raised to 256 KB. Spec `docs/superpowers/specs/2026-06-24-passive-session-preview-design.md`. Browser-verified.
- ✅ **Temporary sessions + rename (merged to `master`)** — (1) one-off **temporary sessions** live under `…\cockpit\_temporary-sessions` (each in its own auto-named subfolder), excluded from the project picker, shown in a distinct **Temporary** sidebar group, created via a **+ Temporary session** button in the New-session picker, listed in a separate **Temporary** section of the Resume modal, and labeled by Claude Code's `aiTitle` (timestamp placeholder → real title via a low-frequency transcript poll). No auto-deletion (persist + resume). (2) **Rename** any session via the right-click context menu — an in-memory custom display label (precedence: customName > aiTitle > folder basename), lost on server restart/resume. Spec `docs/superpowers/specs/2026-06-24-temp-sessions-and-rename-design.md`. 72/72 tests; browser-verified.
- ✅ **Navigation improvements (merged to `master`)** — (A) right-click **Open folder** opens the session's directory in Windows Explorer (the server resolves the cwd from the registry; injectable opener). (B) a **search box** in the New-session and Resume modals filters live by project name / session title / folder path. (C) the New-session **project picker** lists projects alphabetically within last-used **time bands** (Last 24h / 1–3d / 3–7d / Older / Never used), each row showing the project's last-used time (`GET /api/projects` now returns `lastActivity`, computed by `recent.lastActivityByPath`). Also: the modals now close on **Escape**. Spec `docs/superpowers/specs/2026-06-24-nav-improvements-design.md`. 75/75 tests; browser-verified.
- ✅ **Resume scope switch + temp name format + uniform modals (merged to `master`)** — (A) the Resume modal has a **Global discovery / Cockpit / Temporary** scope switch (each `/api/recent` group is tagged `temp`/`cockpit` via `projects.isUnderProjectsRoot`). (B) temp folders are named `YYYY-MM-DD HH-MM-SS`. (C) the interactive modals (New session / Resume / Rename) share one fixed size with inner-scroll on overflow (Quick preview excluded). Spec `docs/superpowers/specs/2026-06-24-resume-scope-and-modal-polish-design.md`. 76/76 tests; browser-verified.
- ✅ **No-transcript bug fixed (TODO B2, merged to `master` 2026-06-25)** — cockpit-spawned sessions wrote no transcript to `~/.claude/projects`, so they never appeared in Resume discovery, projects read "never used", and temp sessions never auto-named. ROOT CAUSE was **env inheritance, not the trust prompt** (the earlier trust-prompt note was wrong): starting the cockpit server from inside a Claude Code session leaks that parent session's `CLAUDE_CODE_CHILD_SESSION=1` marker, which `buildSpawn`'s `{ ...process.env }` passed to every spawned `claude` — and a `claude` that sees that flag treats itself as a nested child session and does not persist a transcript. Proven by a controlled node-pty differential (stripping `CLAUDE_CODE_CHILD_SESSION` alone restores persistence; the effect is interactive-mode-only, which is why earlier `-p` print-mode probes never reproduced it). FIX: `server/pty.js` `scrubParentClaudeEnv()` strips `CLAUDECODE`, the `CLAUDE_CODE_*` namespace, and `CLAUDE_EFFORT`/`AI_AGENT` from the spawn env so each session launches like a fresh top-level `claude`. 77/77 tests; verified end-to-end. This unblocks the rich-frontend work (TODO B1).
- 🚧 **SDK re-architecture + GUI build (branch `feat/agent-sdk-rearch`, not yet merged)** — cc-cockpit was re-founded on the **Claude Agent SDK**: each session is driven through one durable `query()` (structured input/output + tool-permission control), and the **PTY/terminal/`xterm` substrate was removed entirely — SDK-only, no fallback** (TODO section G, complete). Built on top: **A1** image paste/upload in the compose box; **A2** native handling of interactive prompts (permission / AskUserQuestion / plan-accept / MCP elicitation) through the SDK control channel; **A3** a "never-used projects" warning in the New-session picker; **C1** a header chip showing 5h / 7d / context-window usage. 137/137 tests; each feature browser-verified. Specs/plans under `docs/superpowers/{specs,plans}/`.

## How to resume (in a fresh session opened here)

cc-cockpit is built and runs on the Claude Agent SDK. To run it: `npm install` (first time only) then `npm start`, and open `http://127.0.0.1:4477`. To extend it, the backlog lives in `TODO.md` (managed by the `/todo` skill); the design specs and implementation plans in `docs/superpowers/` remain the reference for how the code is structured and why.

## Common commands

- Install dependencies: `npm install`
- Run tests (Node built-in runner, zero test deps): `npm test`
- Start the app: `npm start` → open `http://127.0.0.1:4477`

## Architecture (summary — full detail in the spec)

A Node server drives each `claude` session through the **Claude Agent SDK**'s `query()` — one durable streaming `query()` per session, which spawns and owns the child `claude` over stdio — and bridges the structured conversation to a single web page over a WebSocket. Core logic (the session registry and the incremental conversation fold in `server/normalize.js`) is dependency-injected and event-emitting, so it is unit-testable with an injected fake `query()` and no real `claude`; the SDK driver, the normalize fold, uploads, and the server wiring get unit/integration tests; the browser GUI is verified live against the spec's acceptance criteria. Files: `server/{sdk,sessions,normalize,app,index,hooks,projects,recent,transcript,topics,uploads}.js`, `public/{index.html,app.js,gui.js,compose.js,styles.css}`, `test/*.test.js`.

## Conventions

- Plain Node, **no bundler/build step**. Test runner: built-in `node --test`.
- Keep files small and single-responsibility.
- **Committing (assistant) — commit per step, no asking:** commit frequently as the normal close-out of a verified step — **one commit per completed plan step / TODO item**. This is unconditional: **do NOT ask whether to commit.** This project's commit convention **overrides any generic "only commit/push when the user asks" default** — a specific project guideline outranks a generic default (standing user guideline 2026-06-29, after asking-to-commit was flagged as redundant). Only pause to ask for genuinely irreversible or off-convention actions — history rewrites, force-push, or a change of branching strategy.
- **Branching / isolation — driven by parallelism, not change size:** sequential single-threaded work commits **straight to `main`** (the default — no branch). Isolate on a branch only when work needs isolation: (a) several **independent** features developed **concurrently**, or (b) large / risky multi-step work. Then give each feature its **own branch — preferably a git worktree** (a separate checkout so concurrent edits/builds/tests don't collide; the worktree-setup and branch-finishing skills are the playbook), commit per step inside it, and once the feature is verified **merge it straight to `main` — no PR** (solo local repo; standing user guideline 2026-06-29), then drop the worktree. Practical note: running two cockpit instances at once needs distinct ports (set the `PORT` override per worktree), since the default port collides.
- Server binds **`127.0.0.1` only**, default port `4477` (override via `PORT` env). The UI is equivalent to shell access — never expose the port to the network.
- **Restarting the dev server (assistant) — ALWAYS restart after changes, no asking:** after any fix/modification, the assistant restarts the running `npm start` cockpit itself so the user always sees up-to-date results. This is unconditional — **do NOT wait for confirmation even when the main cockpit has live Claude sessions** (restarting kills them, since the server owns each session's SDK child `claude` process; that is accepted by standing user guideline 2026-06-25). A running Node server does not pick up server-side (`server/*.js`) edits otherwise; client (`public/*`) edits only need a browser reload but restarting anyway is fine. Throwaway test instances (other ports) can be restarted freely.

## Prerequisites / gotchas

- Node.js v22+ (this machine has v24).
- The **Claude Agent SDK** (`@anthropic-ai/claude-agent-sdk`) installs from npm and bundles its own version-pinned `claude` binary (spawned over stdio) — there is no native module to compile. Verify with `node -e "require('@anthropic-ai/claude-agent-sdk'); console.log('ok')"`.
- The `test` script is `node --test --test-force-exit`. The `--test-force-exit` flag is kept defensively: a test that loads the real SDK (the contract smoke test) can leave an async handle alive on Windows, which would otherwise keep the test-runner process from exiting. The suite itself passes without it; the flag just guarantees a clean exit. App behavior is unaffected (the server is long-lived by design).

## Non-goals for v0 — do NOT build these

Originally deferred for v0: split/grid multi-session view; precise "needs input" detection; desktop notifications/sound; persistence across server restart; WebSocket auto-reconnect/backoff; authentication; tab colors/reorder. (Two items from this list — auto-discovery of `~/.claude` sessions and session rename — have since shipped in v0.2+.) Sessions are started **from the cockpit**, not from terminal tabs — that is the one accepted behavior change.
